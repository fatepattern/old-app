let initialState = {
    friends : [
        {id : 0, name : 'Dima'},
        {id : 1, name : 'Oleg'},
        {id : 2, name : 'Max'}
    ]
}

export const sidebarReducer = (state = initialState, action) => {
    return state;
}